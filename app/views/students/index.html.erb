<h3>Search for students</h3>
<%= form_tag students_path, method: :get do %>
  <%= text_field_tag :query, params[:query] %>
  <%= submit_tag ("Search") %>
<% end %>

<ul>
  <% @students.each do |stud| %>
    <li><%= render partial: "student", locals: {student: stud} %></li>
  <% end %>
</ul>